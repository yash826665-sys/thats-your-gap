# That's Your Gap has two separate env files, one per app (this keeps
# server secrets like the LLM API key out of anything the frontend
# build could ever bundle):
#
#   backend/.env.example   → copy to backend/.env
#   frontend/.env.local.example → copy to frontend/.env.local
#
# See docs in each file for details.
