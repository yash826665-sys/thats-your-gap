# Copy this file to frontend/.env.local
# URL of the backend API. No secrets belong here — this file is exposed
# to the browser via NEXT_PUBLIC_ prefix, which is intentional: it's just
# a URL, never an API key.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
